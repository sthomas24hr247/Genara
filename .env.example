# Azure SQL Server connection
DB_SERVER=your-server.database.windows.net
DB_NAME=genara
DB_USER=your-username
DB_PASSWORD=your-password
DB_PORT=1433

# Alternative: Full connection string (optional, used by some tools)
# DATABASE_URL=Server=your-server.database.windows.net;Database=genara;User Id=your-username;Password=your-password;Encrypt=true

# Server port (Azure sets this automatically via PORT env var)
PORT=5000

# Node environment
NODE_ENV=development
